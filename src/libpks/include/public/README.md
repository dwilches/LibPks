When distributing this library, this is the header that gets distributed along with it.

This is done this way to control what the public interface of the library is.
Some of the classes/methods are considered implementation details, but users don't need to know about them.
